'use strict';

describe('only', () => {
    /*
    it.only('should only run this test', () => {
    });

    */
    it('should not run this test', () => {
    });

    /*
    it.only('should run this test too', () => {
    });
    */

    /*
    describe.only('describe only', () => {
        it('should run this test in describe only', () => {
        });

        it('should run this test too in describe only', () => {
        });
    });
    */
});